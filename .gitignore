# build artifacts
__pycache__/
*.pyc

# local probes / research files (scraped site bundles, one-off scripts)
_probe/

# runtime churn (polestar4_inventory.sqlite3 IS tracked: GitHub Actions needs
# it in the repo to build each daily snapshot on top of the last one;
# transient sidecar files and logs stay out)
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm
tracker.log
*.out
